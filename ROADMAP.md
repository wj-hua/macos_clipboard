# 常用粘贴板开发路线图

本文档记录计划开发的功能及优先级。产品继续以“收藏少量重要文本，并快速找到和粘贴”为核心定位，暂不默认收集用户的完整剪贴板历史。

## P0：优先开发

- [x] 文本搜索
  - [x] 支持打开面板后直接输入关键词搜索。
  - [x] 支持搜索当前标签和全部标签。
  - [x] 搜索结果继续支持上下键选择、回车粘贴。
  - [x] `Command + F` 聚焦搜索框；`Esc` 先清空搜索，再关闭面板。

- [ ] 仅复制，不自动粘贴
  - 使用 `Command + C` 将选中项目复制到系统剪贴板。
  - 在项目右键菜单中增加“复制”。
  - 没有辅助功能权限时，仍允许用户仅复制文本。
  - 设置中允许选择双击执行“自动粘贴”或“仅复制”。

- [ ] 在标签之间移动文本
  - 项目右键菜单增加“移动到标签”。
  - 编辑文本时可以选择所属标签。
  - 支持将项目拖放到目标标签。
  - 第一版继续保持一个项目只属于一个标签。

- [ ] 基础设置页
  - 自定义全局快捷键。
  - 开机自动启动。
  - 设置粘贴后是否关闭面板。
  - 设置默认打开上次使用的标签。
  - 设置面板显示位置，并可记住上次位置。
  - 设置双击行为和界面外观。

## P1：重要增强

- [ ] 数据导入、导出与备份
  - 导出和导入完整 JSON 数据。
  - 导入时支持“合并”和“覆盖”。
  - 支持从纯文本或 CSV 批量导入。
  - 修改数据前保留最近几份自动备份。
  - 文件损坏或保存失败时向用户显示错误和恢复入口。

- [ ] 文本标题与关键词
  - 为长文本增加可选标题，列表优先显示标题。
  - 支持为项目添加搜索关键词。
  - 旧数据继续使用正文第一行作为默认预览。

- [ ] 收藏、置顶与最近使用
  - 支持收藏和置顶项目。
  - 增加最近使用视图和使用次数记录。
  - 保留现有手动排序，不默认根据使用频率改变列表顺序。

- [ ] 模板变量
  - 支持 `{name}` 等需要粘贴前填写的变量。
  - 支持 `{date}`、`{time}` 等系统变量。
  - 后续评估 `{clipboard}` 和当前选中文本等高级变量。

## P2：中长期功能

- [ ] iCloud 同步
  - 在数据格式版本、备份和冲突策略完成后再开发。
  - 需要处理多台设备同时修改、删除和重新排序的冲突。

- [ ] 可选剪贴板历史模式
  - 默认关闭，由用户主动启用。
  - 支持容量限制、自动过期和一键清空。
  - 支持排除指定 App，并过滤密码、验证码等敏感内容。
  - 开发前需要完成明确的隐私设计。

- [ ] 图片与富文本收藏
  - 扩展现有纯文本数据模型、存储和预览能力。
  - 评估图片文件大小、备份和同步成本后再决定范围。

## 开发前的技术基础

- [ ] 为持久化数据增加 `schemaVersion`，建立明确的数据迁移流程。
- [ ] 保存失败时不再静默忽略，向界面传递可恢复的错误。
- [ ] 数据文件损坏时保留原文件，并提供从备份恢复的能力。
- [ ] 拆分体积较大的 `ClipboardPanelView.swift`，分离列表、标签栏、编辑器和弹窗视图。
- [ ] 为搜索、移动标签、导入导出和数据迁移补充单元测试。

## 推荐版本顺序

### 第一阶段：快速使用体验

- [x] 文本搜索
- [ ] 仅复制
- [ ] 移动文本到其他标签
- [ ] 记住上次标签和面板位置

### 第二阶段：可配置与数据安全

- [ ] 设置页
- [ ] 自定义快捷键
- [ ] 开机启动
- [ ] 导入、导出和自动备份

### 第三阶段：内容组织与自动化

- [ ] 标题和关键词
- [ ] 收藏及最近使用
- [ ] 模板变量

## 暂不优先

- 完整剪贴板历史：会改变产品定位，并引入敏感内容过滤、隐私和后台性能问题。
- 图片和富文本：当前数据与粘贴流程以纯文本为核心，改造范围较大。
- iCloud 同步：需要先建立可靠的数据版本、备份和冲突处理机制。
